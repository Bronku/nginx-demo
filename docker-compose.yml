services:
  dotnet-1:
    build: ./dotnet
    environment:
      - NAME=Fili
  dotnet-2:
    build: ./dotnet
    environment:
      - NAME=Kili
  java-1:
    build: ./java
    environment:
      - NAME=FiliJava
  java-2:
    build: ./java
    environment:
      - NAME=KiliJava
  nginx:
    image: nginx
    volumes:
      - ./nginx/html:/usr/share/nginx/html:ro
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "80:80"
